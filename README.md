toy-json
--------

This parses a JSON string. Don't use it, there are better libraries out
there for this purpose. I wrote this because I felt like it.

JSON is very simple to parse. It has no operators, no contextual syntax,
no significant whitespace. So, this is a simple recursive descent parser.
